# bascal
BASCAL is a Rust-based compiler/linker for a structured extension of classic Microsoft BASIC, adding multiline IF, functions, explicit return, path-style dependency resolution, separate compilation, and library linking while preserving BASIC’s global symbol model.
